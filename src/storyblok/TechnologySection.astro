---
const { blok } = Astro.props;
console.log(blok);
---
<div class="mt-24">
    <h2 class="text-center text-slate-500">{blok.title}</h2>
    <div
      class="flex gap-8 md:gap-20 items-center justify-center mt-10 flex-wrap"
    >
    
     
    {blok.column.map((item: { svg: string, component: string }) => (
      <div set:html={item.svg} />
  
  ))}
      
     
    </div>
  </div>